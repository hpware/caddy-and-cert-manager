# Setup dev server.
In order to make it dev system work without errors, you should run `./setup_server.sh` in order to setup the caddy and cert env. This file is and is for prod and dev servers. you should run it before starting to dev.
